# === AI API Settings ===
# Multi-provider support for AI services

# AI Provider Selection
# Supported providers: gemini, claude, openai
AI_PROVIDER=gemini

# Primary API Key (for backward compatibility)
AI_API_KEY=your-api-key-here

# free API Key
AI_API_KEY_FREE=your-free-api-key-here # gemini free tier

# Paid API Key for large payloads (>9MB)
# Use a separate API key with higher quotas for batch processing
# Leave empty if you only process files ≤9MB
AI_API_KEY_PAID=your-paid-api-key-here # google project/ paid

# Model Selection (provider-specific)
# Gemini: gemini-2.5-pro, gemini-1.5-pro, gemini-1.5-flash
# Claude: claude-3-opus-20240229, claude-3-sonnet-20240229, claude-3-haiku-20240307
# OpenAI: gpt-4o, gpt-4o-mini, gpt-4-turbo
AI_MODEL=gemini-2.5-pro

# === Gemini Token Pricing (as of 2025-08-16) ===
# Conservative pricing based on >200k tokens tier (higher cost)
# Per 1000 tokens in USD. Reference: https://ai.google.dev/pricing
GEMINI_INPUT_COST_PER_1K=0.0025      # USD per 1000 input tokens
GEMINI_OUTPUT_COST_PER_1K=0.015      # USD per 1000 output tokens  
GEMINI_THOUGHTS_COST_PER_1K=0.0025   # USD per 1000 thoughts tokens (same as input)

# === slack ===

# Slack Integration Settings (Optional - will work without these)
# Get your Bot Token from https://api.slack.com/apps
SLACK_BOT_TOKEN=xoxb-your-workspace-your-token

# Report channel ID (where progress reports are sent)
# Find channel ID: Right-click channel > View channel details > ID at bottom
SLACK_REPORT_CHANNEL=C1234567890

# Human user ID for mentions (user id format)
# Find user ID: Click user profile > More > Copy member ID
SLACK_MENTION_USER_ID=U1234567890

# === PDF Scanner Settings ===
# Configurable content base directory for vault/ structure support
CONTENT_BASE_DIR=vault
# Directory to scan for PDFs (absolute path to avoid path resolution issues)
PDF_INPUT_DIR="/path/to/your/obsidian/scan/unprocessed"

# Temp directory will be created directly under script/ directory
# (no longer using processed directory)
PDF_TEMP_BASE_DIR="./"

# Output directories for processed files (relative to vault directory)
# For scanPDF.sh - local PDF files from scanner
SCAN_OUTPUT_DIR="scan"

# For clipPDF.sh - PDFs downloaded from URLs
CLIP_OUTPUT_DIR="clip"


# For paper category - Academic papers and research documents
PAPER_OUTPUT_DIR="paper"

# PDF attachments directory (relative to vault directory)
ATTACHMENTS_DIR="attachments"

# Processing mode: BATCH or REALTIME
# BATCH: Lower cost, may take a few minutes
# REALTIME: Higher cost, immediate processing
PDF_PROCESSING_MODE="BATCH"

# Maximum parallel processing jobs
MAX_PARALLEL_JOBS=5

# Enable/disable project notifications (true/false)
# Set to false if Slack is not configured
SLACK_PROJECT_NOTIFICATIONS=true

